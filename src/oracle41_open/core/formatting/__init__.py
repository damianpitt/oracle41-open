"""Reserve a package for shared display formatting.

Views currently keep small presentation-only formatting helpers close to their widgets.
Future common formatters can live here without changing domain models.
"""
